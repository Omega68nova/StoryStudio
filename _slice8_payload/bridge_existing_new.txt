        if existing:
            if int(existing.get("legacy_import_complete") or 0):
                return self.batch.get_plan(existing["id"])
            plan = self.sync_session(existing["id"], session_id)
            self.db.execute(
                "UPDATE generation_plans SET legacy_import_complete=1 "
                "WHERE id=?",
                (existing["id"],),
            )
            return self.batch.get_plan(existing["id"])
