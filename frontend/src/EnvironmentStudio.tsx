import { useCallback, useEffect, useMemo, useState } from "react";
import { Button, Checkbox, Divider, FormControlLabel, IconButton, MenuItem, Switch, TextField, Tooltip } from "@mui/material";
import DeleteOutlineIcon from "@mui/icons-material/DeleteOutline";
import { api } from "./api";
import { BoxedMultiselectFilter, CreatableBoxedMultiselect } from "./customComponents/BoxedMultiselect";
import { RecordDrawer, ResourceButton, ResourceList } from "./customComponents/AdminResourceForms";
import { FavoriteLibraryButton } from "./FavoriteLibraryButton";
import type { AmbientAssignment, AmbientSoundSet, AmbientVariant, EnvironmentLocation, EnvironmentSettings, LocationMapLayer, NoiseVariant, TimePhase, WeatherDefinition, WorkflowPreset, WorldEntity, WorldProjection } from "./types";

type Editor = { kind: "weather"; value: WeatherDefinition } | { kind: "time"; value: TimePhase } | { kind: "location"; value: EnvironmentLocation };
type AmbientData = { variants: AmbientVariant[]; assignments: AmbientAssignment[] };
const emptySet = (): AmbientSoundSet => ({ selector_type: "default", selector_value: null, weather_id: null, time_phase_id: null, variant_ids: [] });
const emptyLocation = (parentId: string | null): EnvironmentLocation => ({ name: "", parent_location_id: parentId, exposure: "outdoor", description: "", imagegen_description: "", tags: [], image_tags: [], enabled: true, random_encounter: false, hidden: false, discovered: true, x: null, y: null, topology: "closed", occupancy: "direct_allowed", boundary_access: "free", spatial_kind: "spot", priority_layer: 0, minutes_per_unit: 1, base_visibility_units: null, encounter_rate: 0, footprint: null, local_bounds: null });

async function environmentRequest<T>(label: string, request: Promise<T>): Promise<T> {
  try { return await request; }
  catch (cause) { throw new Error(`${label}: ${cause instanceof Error ? cause.message : String(cause)}`); }
}

function setsFor(assignments: AmbientAssignment[], ownerType: string, ownerId?: string): AmbientSoundSet[] {
  if (!ownerId) return [emptySet()];
  const grouped = new Map<string, AmbientSoundSet>();
  assignments.filter(item => item.owner_type === ownerType && item.owner_id === ownerId).forEach(item => {
    const key = [item.selector_type, item.selector_value ?? "", item.weather_id ?? "", item.time_phase_id ?? ""].join("|");
    const current = grouped.get(key) ?? { selector_type: item.selector_type, selector_value: item.selector_value, weather_id: item.weather_id, time_phase_id: item.time_phase_id, variant_ids: [] };
    if (!current.variant_ids.includes(item.variant_id)) current.variant_ids.push(item.variant_id);
    grouped.set(key, current);
  });
  const result = [...grouped.values()];
  if (!result.some(item => item.selector_type === "default" && !item.weather_id && !item.time_phase_id)) result.unshift(emptySet());
  return result;
}

function normalizedLocation(entity: WorldEntity): EnvironmentLocation {
  const state = entity.state;
  return {
    id: entity.id, name: entity.name, tags: entity.tags ?? [], parent_location_id: String(state.parent_location_id || "") || null,
    exposure: state.exposure === "indoor" || state.exposure === "isolated" ? state.exposure : "outdoor",
    description: String(state.description ?? ""), imagegen_description: String(state.imagegen_description ?? ""),
    image_tags: Array.isArray(state.image_tags) ? state.image_tags.map(String) : [], enabled: state.enabled !== false,
    random_encounter: Boolean(state.random_encounter), hidden: Boolean(state.hidden), discovered: state.discovered == null ? !state.random_encounter : Boolean(state.discovered),
    x: typeof state.x === "number" ? state.x : null, y: typeof state.y === "number" ? state.y : null,
    topology: state.topology === "open" ? "open" : "closed",
    occupancy: state.occupancy === "child_required" ? "child_required" : "direct_allowed",
    boundary_access: state.boundary_access === "connection_required" ? "connection_required" : "free",
    spatial_kind: state.spatial_kind === "area" ? "area" : "spot",
    priority_layer: typeof state.priority_layer === "number" ? state.priority_layer : 0,
    minutes_per_unit: typeof state.minutes_per_unit === "number" ? state.minutes_per_unit : 1,
    base_visibility_units: typeof state.base_visibility_units === "number" ? state.base_visibility_units : null,
    encounter_rate: typeof state.encounter_rate === "number" ? state.encounter_rate : 0,
    footprint: state.footprint && typeof state.footprint === "object" ? state.footprint as Record<string, unknown> : null,
    local_bounds: state.local_bounds && typeof state.local_bounds === "object" ? state.local_bounds as Record<string, unknown> : null,
  };
}

export function EnvironmentStudio({ projectId, revision, workflows, fail, focusLocationId, onFocusHandled }: { projectId: string; revision: number; workflows: WorkflowPreset[]; fail: (message: string) => void; focusLocationId?: string | null; onFocusHandled?: () => void }) {
  const [settings, setSettings] = useState<EnvironmentSettings | null>(null);
  const [map, setMap] = useState<LocationMapLayer | null>(null);
  const [parentId, setParentId] = useState<string | null>(null);
  const [ambient, setAmbient] = useState<AmbientData>({ variants: [], assignments: [] });
  const [noises, setNoises] = useState<NoiseVariant[]>([]);
  const [world, setWorld] = useState<WorldProjection | null>(null);
  const [proposals, setProposals] = useState<Array<{ id: string; name: string; description: string; status: string }>>([]);
  const [editor, setEditor] = useState<Editor | null>(null);
  const [soundSets, setSoundSets] = useState<AmbientSoundSet[]>([emptySet()]);
  const [initialDraft, setInitialDraft] = useState("");
  const [formError, setFormError] = useState("");
  const [search, setSearch] = useState({ weather: "", time: "", location: "" });
  const [derived, setDerived] = useState({ source_path: "", label: "", playback_rate: 2, default_gain: 1 });
  const [actionRule, setActionRule] = useState({ owner_id: "", variant_id: "" });
  const [background, setBackground] = useState({ weather_id: "", time_phase_id: "", prompt: "" });

  const load = useCallback(async () => {
    const [nextSettings, nextMap, nextAmbient, nextNoises, nextWorld, nextProposals] = await Promise.all([
      environmentRequest("Environment settings", api<EnvironmentSettings>(`/projects/${projectId}/environment/settings`)),
      environmentRequest("Location map", api<LocationMapLayer>(`/projects/${projectId}/environment/map${parentId ? `?parent_id=${parentId}` : ""}`)),
      environmentRequest("Ambient sounds", api<AmbientData>(`/projects/${projectId}/environment/ambient`)),
      environmentRequest("One-shot noises", api<NoiseVariant[]>(`/projects/${projectId}/noises`)),
      environmentRequest("World projection", api<WorldProjection>(`/projects/${projectId}/world`)),
      environmentRequest("Weather proposals", api<Array<{ id: string; name: string; description: string; status: string }>>(`/projects/${projectId}/environment/weather-proposals`)),
    ]);
    setSettings(nextSettings); setMap(nextMap); setAmbient(nextAmbient); setNoises(nextNoises); setWorld(nextWorld); setProposals(nextProposals);
  }, [projectId, parentId]);
  useEffect(() => { void load().catch(cause => fail(String(cause))); }, [load, revision, fail]);

  const locations = useMemo(() => Object.values(world?.entities ?? {}).filter(item => item.kind === "location" && !item.state.archived), [world]);
  const allTags = useMemo(() => [...new Set([...(settings?.weather.flatMap(item => [...item.tags, ...item.image_tags]) ?? []), ...locations.flatMap(item => [...item.tags, ...(Array.isArray(item.state.image_tags) ? item.state.image_tags.map(String) : [])]), ...ambient.variants.flatMap(item => item.tags)])].sort((a, b) => a.localeCompare(b)), [settings, locations, ambient.variants]);
  const snapshot = editor ? JSON.stringify({ editor, soundSets }) : "";
  const dirty = Boolean(editor && initialDraft && snapshot !== initialDraft);

  function begin(next: Editor, ownerType: "weather" | "time" | "location", ownerId?: string) {
    const nextSets = setsFor(ambient.assignments, ownerType, ownerId);
    setEditor(next); setSoundSets(nextSets); setInitialDraft(JSON.stringify({ editor: next, soundSets: nextSets })); setFormError("");
    if (next.kind === "location") setBackground({ weather_id: "", time_phase_id: "", prompt: composedPrompt(next.value, settings) });
  }
  useEffect(() => {
    if (!focusLocationId || !world?.entities[focusLocationId] || !settings) return;
    const entity = world.entities[focusLocationId];
    if (entity.kind !== "location") return;
    setParentId(String(entity.state.parent_location_id || "") || null);
    begin({ kind: "location", value: normalizedLocation(entity) }, "location", entity.id);
    onFocusHandled?.();
  // This consumes an explicit cross-workspace navigation request exactly once.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [focusLocationId, world, settings]);
  function closeEditor(force = false) {
    if (!force && dirty && !window.confirm("Discard unsaved environment changes?")) return;
    setEditor(null); setInitialDraft(""); setFormError("");
  }
  async function saveSettings(patch: Partial<EnvironmentSettings>) {
    if (!settings) return;
    try { setSettings(await api(`/projects/${projectId}/environment/settings`, { method: "PUT", body: JSON.stringify({ ...settings, ...patch }) })); } catch (cause) { fail(String(cause)); }
  }
  async function saveEditor() {
    if (!editor || !settings) return;
    setFormError("");
    try {
      let ownerId = editor.value.id;
      if (!editor.value.name.trim()) throw new Error("Name is required");
      if (editor.kind === "weather") {
        const body = { name: editor.value.name, description: editor.value.description, imagegen_description: editor.value.imagegen_description, tags: editor.value.tags, image_tags: editor.value.image_tags, enabled: editor.value.enabled, visibility_multiplier: editor.value.visibility_multiplier ?? 1 };
        const saved = ownerId ? await api<WeatherDefinition>(`/projects/${projectId}/environment/weather/${ownerId}`, { method: "PUT", body: JSON.stringify(body) }) : await api<WeatherDefinition>(`/projects/${projectId}/environment/weather`, { method: "POST", body: JSON.stringify(body) });
        ownerId = saved.id; await api(`/projects/${projectId}/environment/weather/${ownerId}/transitions`, { method: "PUT", body: JSON.stringify({ target_weather_ids: editor.value.transition_ids }) });
      } else if (editor.kind === "time") {
        if (editor.value.duration_minutes < 1) throw new Error("Duration must be at least one minute");
        const saved = ownerId ? await api<TimePhase>(`/projects/${projectId}/environment/time-phases/${ownerId}`, { method: "PUT", body: JSON.stringify(editor.value) }) : await api<TimePhase>(`/projects/${projectId}/environment/time-phases`, { method: "POST", body: JSON.stringify({ ...editor.value, id: null }) });
        ownerId = saved.id;
      } else {
        const saved = ownerId ? await api<WorldEntity>(`/projects/${projectId}/environment/locations/${ownerId}`, { method: "PUT", body: JSON.stringify(editor.value) }) : await api<WorldEntity>(`/projects/${projectId}/environment/locations`, { method: "POST", body: JSON.stringify(editor.value) });
        ownerId = saved.id;
      }
      await api(`/projects/${projectId}/environment/ambient/assignments/${editor.kind}/${ownerId}`, { method: "PUT", body: JSON.stringify({ sets: soundSets.filter(item => item.variant_ids.length) }) });
      await load(); closeEditor(true);
    } catch (cause) { setFormError(String(cause)); }
  }
  async function deleteEditor() {
    if (!editor || !editor.value.id || !window.confirm(`Delete ${editor.value.name}?`)) return;
    try { await api(`/projects/${projectId}/environment/${editor.kind === "weather" ? `weather/${editor.value.id}` : `time-phases/${editor.value.id}`}`, { method: "DELETE" }); await load(); closeEditor(true); } catch (cause) { setFormError(String(cause)); }
  }
  async function reorderTime(id: string, direction: -1 | 1) {
    if (!settings) return; const ids = settings.time_phases.map(item => item.id), index = ids.indexOf(id), target = index + direction; if (target < 0 || target >= ids.length) return;
    [ids[index], ids[target]] = [ids[target], ids[index]];
    try { await api(`/projects/${projectId}/environment/time-phase-order`, { method: "PUT", body: JSON.stringify({ phase_ids: ids }) }); await load(); } catch (cause) { fail(String(cause)); }
  }
  async function moveLocation(id: string, clientX: number, clientY: number, element: HTMLElement) {
    const entity = world?.entities[id], rect = element.parentElement?.getBoundingClientRect(); if (!entity || !rect) return;
    const value = normalizedLocation(entity); value.x = Math.round(((clientX - rect.left) / rect.width * 100 - 10) / 21); value.y = Math.round(((clientY - rect.top) / rect.height * 100 - 12) / 24);
    try { await api(`/projects/${projectId}/environment/locations/${id}`, { method: "PUT", body: JSON.stringify(value) }); await load(); } catch (cause) { fail(String(cause)); }
  }
  async function decideProposal(id: string, action: "approve" | "reject") { try { await api(`/projects/${projectId}/environment/weather-proposals/${id}`, { method: "PUT", body: JSON.stringify({ action }) }); await load(); } catch (cause) { fail(String(cause)); } }
  async function createDerived() { if (!derived.source_path || !derived.label) return; try { await api(`/projects/${projectId}/environment/ambient/variants`, { method: "POST", body: JSON.stringify({ ...derived, tags: [], enabled: true }) }); setDerived({ source_path: "", label: "", playback_rate: 2, default_gain: 1 }); await load(); } catch (cause) { fail(String(cause)); } }
  async function saveVariant(item: AmbientVariant, patch: Partial<AmbientVariant>) { try { await api(`/projects/${projectId}/environment/ambient/variants/${item.id}`, { method: "PUT", body: JSON.stringify({ ...item, ...patch }) }); await load(); } catch (cause) { fail(String(cause)); } }
  async function saveNoise(item: NoiseVariant, patch: Partial<NoiseVariant>) { try { await api(`/projects/${projectId}/noises/${item.id}`, { method: "PUT", body: JSON.stringify({ ...item, ...patch }) }); await load(); } catch (cause) { fail(String(cause)); } }
  async function addActionRule() { if (!actionRule.owner_id.trim() || !actionRule.variant_id) return; try { await api(`/projects/${projectId}/environment/ambient/assignments`, { method: "POST", body: JSON.stringify({ owner_type: "action", owner_id: actionRule.owner_id.trim(), selector_type: "default", variant_id: actionRule.variant_id }) }); setActionRule({ owner_id: "", variant_id: "" }); await load(); } catch (cause) { fail(String(cause)); } }
  async function generateBackground() {
    const currentSettings = settings;
    if (!editor || editor.kind !== "location" || !editor.value.id || !currentSettings?.background_workflow_id) return; const prompt = background.prompt.trim() || composedPrompt(editor.value, currentSettings);
    try { const created = await api<{ media_asset_id: string }>(`/projects/${projectId}/environment/locations/${editor.value.id}/backgrounds`, { method: "POST", body: JSON.stringify({ prompt, negative_prompt: "", weather_id: background.weather_id || null, time_phase_id: background.time_phase_id || null }) }); await api(`/media-assets/${created.media_asset_id}/generate`, { method: "POST", body: JSON.stringify({ workflow_preset_id: currentSettings.background_workflow_id, prompt, negative_prompt: "", width: null, height: null }) }); } catch (cause) { setFormError(String(cause)); }
  }

  if (!settings) return null;
  const filterBy = <T extends { name: string }>(values: T[], key: keyof typeof search) => values.filter(item => item.name.toLocaleLowerCase().includes(search[key].toLocaleLowerCase()));
  return <div className="page environment-page">
    <header className="page-header"><p className="eyebrow">PROJECT ENVIRONMENT SETTINGS</p><h1>Environment settings</h1><p>Weather, time, ambient audio, and transitional location controls. Environment & Map is now the primary location authoring workspace; location controls here remain only until sidebar parity is complete.</p></header>
    <section className="panel environment-settings"><h2>Behavior</h2><FormControlLabel control={<Switch checked={settings.enabled} onChange={e => void saveSettings({ enabled: e.target.checked })} />} label="Environment enabled" /><FormControlLabel control={<Switch checked={settings.ai_create_locations} onChange={e => void saveSettings({ ai_create_locations: e.target.checked })} />} label="AI may create locations" /><FormControlLabel control={<Switch checked={settings.ai_propose_weather} onChange={e => void saveSettings({ ai_propose_weather: e.target.checked })} />} label="AI may propose weather" /><FormControlLabel control={<Switch checked={settings.auto_generate_backgrounds} onChange={e => void saveSettings({ auto_generate_backgrounds: e.target.checked })} />} label="Generate backgrounds on demand" /><TextField select size="small" label="Background workflow" value={settings.background_workflow_id ?? ""} onChange={e => void saveSettings({ background_workflow_id: e.target.value || null, auto_generate_backgrounds: e.target.value ? settings.auto_generate_backgrounds : false })}><MenuItem value="">None</MenuItem>{workflows.map(item => <MenuItem key={item.id} value={item.id}>{item.name}</MenuItem>)}</TextField><TextField size="small" label="Perception stat key" helperText="Optional character stat used to scale map discovery" value={settings.perception_stat_key ?? ""} onBlur={e => void saveSettings({ perception_stat_key: e.target.value.trim() || null })} onChange={e => setSettings({ ...settings, perception_stat_key: e.target.value })} /></section>
    <div className="environment-resource-grid">
      <ResourceList title="Weather" query={search.weather} setQuery={value => setSearch({ ...search, weather: value })} onAdd={() => begin({ kind: "weather", value: { id: "", name: "", description: "", imagegen_description: "", tags: [], image_tags: [], enabled: true, transition_ids: [] } }, "weather")}><TextField select fullWidth size="small" label="Initial weather" value={settings.initial_weather_id} onChange={e => void saveSettings({ initial_weather_id: e.target.value })}>{settings.weather.filter(item => item.enabled).map(item => <MenuItem key={item.id} value={item.id}>{item.name}</MenuItem>)}</TextField>{filterBy(settings.weather, "weather").map(item => <ResourceButton key={item.id} enabled={item.enabled} title={item.name} subtitle={`${item.transition_ids.length} transitions`} onClick={() => begin({ kind: "weather", value: { ...item } }, "weather", item.id)} />)}{proposals.filter(item => item.status === "pending").map(item => <div className="environment-proposal" key={item.id}><span><b>{item.name}</b><small>{item.description}</small></span><Button onClick={() => void decideProposal(item.id, "approve")}>Approve</Button><Button color="error" onClick={() => void decideProposal(item.id, "reject")}>Reject</Button></div>)}</ResourceList>
      <ResourceList title="Time cycle" query={search.time} setQuery={value => setSearch({ ...search, time: value })} onAdd={() => begin({ kind: "time", value: { id: "", name: "", duration_minutes: 60, description: "", imagegen_description: "", position: settings.time_phases.length, enabled: true } }, "time")}>{filterBy(settings.time_phases, "time").map((item, index) => <div className="environment-list-with-order" key={item.id}><ResourceButton enabled={item.enabled} title={item.name} subtitle={`${item.duration_minutes} minutes`} onClick={() => begin({ kind: "time", value: { ...item } }, "time", item.id)} /><span><Button disabled={index === 0} onClick={() => void reorderTime(item.id, -1)}>↑</Button><Button disabled={index === settings.time_phases.length - 1} onClick={() => void reorderTime(item.id, 1)}>↓</Button></span></div>)}</ResourceList>
      <ResourceList title="Locations" query={search.location} setQuery={value => setSearch({ ...search, location: value })} onAdd={() => begin({ kind: "location", value: emptyLocation(parentId) }, "location")}>{filterBy(locations.map(normalizedLocation), "location").map(item => <div className="resource-favorite-row" key={item.id}><ResourceButton enabled={item.enabled} title={item.name} subtitle={item.exposure === "isolated" ? "Sealed" : item.exposure} onClick={() => begin({ kind: "location", value: item }, "location", item.id)} /><FavoriteLibraryButton projectId={projectId} sourceKind="location" sourceKey={item.id!} /></div>)}</ResourceList>
    </div>
    <section className="panel map-panel"><div className="sheet-heading"><h2>{map?.parent?.name ?? "World locations"}</h2>{map?.parent && <Button onClick={() => setParentId(map.parent?.parent_id ?? null)}>Go back</Button>}</div><div className="environment-map"><MapEdges layer={map} />{map?.locations.map(item => <button className={!item.effectively_enabled ? "disabled" : ""} draggable key={item.id} style={{ left: `${10 + item.x * 21}%`, top: `${12 + item.y * 24}%` }} onDragEnd={e => void moveLocation(item.id, e.clientX, e.clientY, e.currentTarget)} onClick={() => { const entity = world?.entities[item.id]; if (entity) begin({ kind: "location", value: normalizedLocation(entity) }, "location", item.id); if (item.has_children) setParentId(item.id); }}><b>{item.name}</b><small>{item.exposure === "isolated" ? "sealed" : item.exposure}{item.has_children ? " · enter" : ""}</small></button>)}</div></section>
    <AmbientCatalog ambient={ambient} derived={derived} setDerived={setDerived} actionRule={actionRule} setActionRule={setActionRule} saveVariant={saveVariant} createDerived={createDerived} addActionRule={addActionRule} />
    <NoiseCatalog noises={noises} saveNoise={saveNoise} />
    <RecordDrawer title={editor ? `${editor.value.id ? "Edit" : "Create"} ${editor.kind}` : ""} open={Boolean(editor)} dirty={dirty} error={formError} onClose={() => closeEditor()} onSave={saveEditor} onDelete={editor?.value.id ? deleteEditor : undefined}>{editor?.kind === "weather" && <WeatherForm value={editor.value} setValue={value => setEditor({ kind: "weather", value })} settings={settings} tags={allTags} soundSets={soundSets} setSoundSets={setSoundSets} variants={ambient.variants} />}{editor?.kind === "time" && <TimeForm value={editor.value} setValue={value => setEditor({ kind: "time", value })} settings={settings} soundSets={soundSets} setSoundSets={setSoundSets} variants={ambient.variants} />}{editor?.kind === "location" && <LocationForm value={editor.value} setValue={value => setEditor({ kind: "location", value })} locations={locations} settings={settings} tags={allTags} soundSets={soundSets} setSoundSets={setSoundSets} variants={ambient.variants} background={background} setBackground={setBackground} onGenerate={generateBackground} projectId={projectId} reload={load} />}</RecordDrawer>
  </div>;
}

function WeatherForm({ value, setValue, settings, tags, soundSets, setSoundSets, variants }: { value: WeatherDefinition; setValue: (value: WeatherDefinition) => void; settings: EnvironmentSettings; tags: string[]; soundSets: AmbientSoundSet[]; setSoundSets: (value: AmbientSoundSet[]) => void; variants: AmbientVariant[] }) {
  const choices = settings.weather.filter(item => item.id !== value.id);
  return <><FormFields name={value.name} description={value.description} imagegen={value.imagegen_description} enabled={value.enabled} setName={name => setValue({ ...value, name })} setDescription={description => setValue({ ...value, description })} setImagegen={imagegen_description => setValue({ ...value, imagegen_description })} setEnabled={enabled => setValue({ ...value, enabled })} enabledDisabled={settings.initial_weather_id === value.id} /><TextField type="number" label="Visibility multiplier" inputProps={{ min: 0, max: 10, step: .1 }} value={value.visibility_multiplier ?? 1} onChange={e => setValue({ ...value, visibility_multiplier: Number(e.target.value) })} /><CreatableBoxedMultiselect label="Tags" tooltipLabel="tags" options={tags} value={value.tags} onChange={(_e, next) => setValue({ ...value, tags: next })} /><CreatableBoxedMultiselect label="Image tags" tooltipLabel="image tags" options={tags} value={value.image_tags} onChange={(_e, next) => setValue({ ...value, image_tags: next })} /><BoxedMultiselectFilter label="Can transition to" tooltipLabel="weather transitions" options={choices} value={choices.filter(item => value.transition_ids.includes(item.id))} onChange={(_e, next) => setValue({ ...value, transition_ids: next.map(item => item.id) })} /><SoundSetsEditor owner="weather" sets={soundSets} setSets={setSoundSets} variants={variants} settings={settings} /></>;
}
function TimeForm({ value, setValue, settings, soundSets, setSoundSets, variants }: { value: TimePhase; setValue: (value: TimePhase) => void; settings: EnvironmentSettings; soundSets: AmbientSoundSet[]; setSoundSets: (value: AmbientSoundSet[]) => void; variants: AmbientVariant[] }) { return <><FormFields name={value.name} description={value.description} imagegen={value.imagegen_description} enabled={value.enabled} setName={name => setValue({ ...value, name })} setDescription={description => setValue({ ...value, description })} setImagegen={imagegen_description => setValue({ ...value, imagegen_description })} setEnabled={enabled => setValue({ ...value, enabled })} /><TextField type="number" label="Duration in minutes" inputProps={{ min: 1 }} value={value.duration_minutes} onChange={e => setValue({ ...value, duration_minutes: Number(e.target.value) })} /><TextField type="number" label="Visibility multiplier" inputProps={{ min: 0, max: 10, step: .1 }} value={value.visibility_multiplier ?? 1} onChange={e => setValue({ ...value, visibility_multiplier: Number(e.target.value) })} /><SoundSetsEditor owner="time" sets={soundSets} setSets={setSoundSets} variants={variants} settings={settings} /></>; }
function FormFields({ name, description, imagegen, enabled, setName, setDescription, setImagegen, setEnabled, enabledDisabled = false }: { name: string; description: string; imagegen: string; enabled: boolean; setName: (value: string) => void; setDescription: (value: string) => void; setImagegen: (value: string) => void; setEnabled: (value: boolean) => void; enabledDisabled?: boolean }) { return <><TextField required label="Name" value={name} onChange={e => setName(e.target.value)} /><TextField multiline minRows={4} label="Description" value={description} onChange={e => setDescription(e.target.value)} /><TextField multiline minRows={3} label="Image-generation description" value={imagegen} onChange={e => setImagegen(e.target.value)} /><FormControlLabel control={<Switch disabled={enabledDisabled} checked={enabled} onChange={e => setEnabled(e.target.checked)} />} label={enabledDisabled ? "Enabled (initial weather)" : "Enabled"} /></>; }

function LocationForm({ value, setValue, locations, settings, tags, soundSets, setSoundSets, variants, background, setBackground, onGenerate, reload }: { value: EnvironmentLocation; setValue: (value: EnvironmentLocation) => void; locations: WorldEntity[]; settings: EnvironmentSettings; tags: string[]; soundSets: AmbientSoundSet[]; setSoundSets: (value: AmbientSoundSet[]) => void; variants: AmbientVariant[]; background: { weather_id: string; time_phase_id: string; prompt: string }; setBackground: (value: { weather_id: string; time_phase_id: string; prompt: string }) => void; onGenerate: () => void; projectId: string; reload: () => Promise<void> }) {
  const blockedParents = descendantsOf(value.id, locations);
  return <><FormFields name={value.name} description={value.description} imagegen={value.imagegen_description} enabled={value.enabled} setName={name => setValue({ ...value, name })} setDescription={description => setValue({ ...value, description })} setImagegen={imagegen_description => setValue({ ...value, imagegen_description })} setEnabled={enabled => setValue({ ...value, enabled })} /><TextField select label="Parent location" value={value.parent_location_id ?? ""} onChange={e => setValue({ ...value, parent_location_id: e.target.value || null })}><MenuItem value="">World root</MenuItem>{locations.filter(item => !blockedParents.has(item.id)).map(item => <MenuItem key={item.id} value={item.id}>{item.name}</MenuItem>)}</TextField><div className="environment-condition-row"><TextField select label="Topology" value={value.topology} onChange={e => setValue({ ...value, topology: e.target.value as EnvironmentLocation["topology"] })}><MenuItem value="open">Open/free movement</MenuItem><MenuItem value="closed">Closed/routed</MenuItem></TextField><TextField select label="Occupancy" value={value.occupancy} onChange={e => setValue({ ...value, occupancy: e.target.value as EnvironmentLocation["occupancy"] })}><MenuItem value="direct_allowed">Direct occupancy allowed</MenuItem><MenuItem value="child_required">Child location required</MenuItem></TextField></div><div className="environment-condition-row"><TextField select label="Boundary access" value={value.boundary_access} onChange={e => setValue({ ...value, boundary_access: e.target.value as EnvironmentLocation["boundary_access"] })}><MenuItem value="free">Free boundary</MenuItem><MenuItem value="connection_required">Door/portal required</MenuItem></TextField><TextField select label="Spatial kind" value={value.spatial_kind} onChange={e => setValue({ ...value, spatial_kind: e.target.value as EnvironmentLocation["spatial_kind"] })}><MenuItem value="spot">Spot</MenuItem><MenuItem value="area">Area</MenuItem></TextField></div><div className="environment-condition-row"><TextField select label="Exposure" value={value.exposure} onChange={e => setValue({ ...value, exposure: e.target.value as EnvironmentLocation["exposure"] })}><MenuItem value="outdoor">Outdoor</MenuItem><MenuItem value="indoor">Indoor</MenuItem><MenuItem value="isolated">Sealed</MenuItem></TextField><TextField type="number" label="Minutes per map unit" inputProps={{ min: .001, step: .1 }} value={value.minutes_per_unit} onChange={e => setValue({ ...value, minutes_per_unit: Number(e.target.value) })} /></div><div className="environment-condition-row"><TextField type="number" label="Visibility radius" value={value.base_visibility_units ?? ""} onChange={e => setValue({ ...value, base_visibility_units: e.target.value === "" ? null : Number(e.target.value) })} /><TextField type="number" label="Encounter rate" inputProps={{ min: 0, max: 1, step: .01 }} value={value.encounter_rate} onChange={e => setValue({ ...value, encounter_rate: Number(e.target.value) })} />{value.spatial_kind === "area" && <TextField type="number" label="Area priority layer" helperText="Lower values win overlaps." value={value.priority_layer} onChange={e => setValue({ ...value, priority_layer: Number(e.target.value) })} />}</div><CreatableBoxedMultiselect label="Tags" tooltipLabel="tags" options={tags} value={value.tags} onChange={(_e, next) => setValue({ ...value, tags: next })} /><CreatableBoxedMultiselect label="Image tags" tooltipLabel="image tags" options={tags} value={value.image_tags} onChange={(_e, next) => setValue({ ...value, image_tags: next })} /><div className="environment-toggle-row"><FormControlLabel control={<Checkbox checked={value.random_encounter} onChange={e => setValue({ ...value, random_encounter: e.target.checked, discovered: e.target.checked ? false : value.discovered })} />} label="Random encounter" /><FormControlLabel control={<Checkbox checked={value.discovered} onChange={e => setValue({ ...value, discovered: e.target.checked })} />} label="Discovered" /></div><SoundSetsEditor owner="location" sets={soundSets} setSets={setSoundSets} variants={variants} settings={settings} />{value.id && <section className="environment-form-section"><h3>Backgrounds</h3><label className="file-button">Upload default background<input hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={async e => { const file = e.target.files?.[0]; if (!file) return; const form = new FormData(); form.append("file", file); await api(`/entities/${value.id}/media/upload?kind=location`, { method: "POST", body: form }); await reload(); }} /></label><div className="environment-condition-row"><TextField select size="small" label="Weather" value={background.weather_id} onChange={e => setBackground({ weather_id: e.target.value, time_phase_id: background.time_phase_id, prompt: composedPrompt(value, settings, e.target.value, background.time_phase_id) })}><MenuItem value="">Any</MenuItem>{settings.weather.map(item => <MenuItem key={item.id} value={item.id}>{item.name}</MenuItem>)}</TextField><TextField select size="small" label="Time" value={background.time_phase_id} onChange={e => setBackground({ weather_id: background.weather_id, time_phase_id: e.target.value, prompt: composedPrompt(value, settings, background.weather_id, e.target.value) })}><MenuItem value="">Any</MenuItem>{settings.time_phases.map(item => <MenuItem key={item.id} value={item.id}>{item.name}</MenuItem>)}</TextField></div><TextField multiline minRows={3} label="Generation prompt" value={background.prompt} onChange={e => setBackground({ ...background, prompt: e.target.value })} /><Button disabled={!settings.background_workflow_id} onClick={() => void onGenerate()}>Generate background</Button></section>}</>;
}

function SoundSetsEditor({ owner, sets, setSets, variants, settings }: { owner: "weather" | "time" | "location"; sets: AmbientSoundSet[]; setSets: (value: AmbientSoundSet[]) => void; variants: AmbientVariant[]; settings: EnvironmentSettings }) {
  const baseIndex = sets.findIndex(item => item.selector_type === "default" && !item.weather_id && !item.time_phase_id), base = baseIndex >= 0 ? sets[baseIndex] : emptySet();
  const custom = sets.map((item, index) => ({ item, index })).filter(({ index }) => index !== baseIndex), selected = (ids: string[]) => variants.filter(item => ids.includes(item.id));
  const update = (index: number, patch: Partial<AmbientSoundSet>) => setSets(sets.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item));
  const soundPicker = (item: AmbientSoundSet, index: number, label: string) => <BoxedMultiselectFilter label={label} tooltipLabel="ambient sounds" options={variants} value={selected(item.variant_ids)} getOptionSecondaryText={(variant: AmbientVariant) => `${variant.playback_rate}× · gain ${variant.default_gain}${!variant.available ? " · missing" : !variant.enabled ? " · disabled" : ""}`} getOptionDisabled={(variant: AmbientVariant) => (!variant.enabled || !variant.available) && !item.variant_ids.includes(variant.id)} onChange={(_e, next) => index >= 0 ? update(index, { variant_ids: next.map(variant => variant.id) }) : setSets([{ ...base, variant_ids: next.map(variant => variant.id) }, ...sets])} />;
  return <section className="environment-form-section"><h3>Ambient sounds</h3><p>These loops are independent from Music.</p>{soundPicker(base, baseIndex, "Default ambient sounds")}{custom.map(({ item, index }) => <div className="environment-sound-set" key={`${index}-${item.selector_type}`}><div className="environment-condition-row"><TextField select size="small" label="Applies in" value={item.selector_type} onChange={e => update(index, { selector_type: e.target.value as AmbientSoundSet["selector_type"], selector_value: null })}>{[["default", "Every exposure"], ["outdoor", "Outdoor"], ["indoor", "Indoor"], ["isolated", "Sealed"], ["tag", "Location tag"]].map(([id, label]) => <MenuItem key={id} value={id}>{label}</MenuItem>)}</TextField>{item.selector_type === "tag" && <TextField size="small" label="Location tag" value={item.selector_value ?? ""} onChange={e => update(index, { selector_value: e.target.value })} />}{owner !== "time" && <TextField select size="small" label="Time" value={item.time_phase_id ?? ""} onChange={e => update(index, { time_phase_id: e.target.value || null })}><MenuItem value="">Any time</MenuItem>{settings.time_phases.map(phase => <MenuItem key={phase.id} value={phase.id}>{phase.name}</MenuItem>)}</TextField>}{owner !== "weather" && <TextField select size="small" label="Weather" value={item.weather_id ?? ""} onChange={e => update(index, { weather_id: e.target.value || null })}><MenuItem value="">Any weather</MenuItem>{settings.weather.map(weather => <MenuItem key={weather.id} value={weather.id}>{weather.name}</MenuItem>)}</TextField>}<Tooltip title="Remove sound set"><IconButton onClick={() => setSets(sets.filter((_, itemIndex) => itemIndex !== index))}><DeleteOutlineIcon /></IconButton></Tooltip></div>{soundPicker(item, index, "Sounds")}</div>)}<Button onClick={() => setSets([...sets, { ...emptySet(), time_phase_id: owner === "weather" ? settings.time_phases[0]?.id ?? null : null, weather_id: owner === "time" ? settings.weather[0]?.id ?? null : null }])}>Add conditional sound set</Button></section>;
}

function AmbientCatalog({ ambient, derived, setDerived, actionRule, setActionRule, saveVariant, createDerived, addActionRule }: { ambient: AmbientData; derived: { source_path: string; label: string; playback_rate: number; default_gain: number }; setDerived: (value: { source_path: string; label: string; playback_rate: number; default_gain: number }) => void; actionRule: { owner_id: string; variant_id: string }; setActionRule: (value: { owner_id: string; variant_id: string }) => void; saveVariant: (item: AmbientVariant, patch: Partial<AmbientVariant>) => Promise<void>; createDerived: () => Promise<void>; addActionRule: () => Promise<void> }) {
  return <section className="panel"><h2>Ambient sound catalog</h2><p>Manage ambient loops here. Record links are edited in their drawers; Music is unaffected.</p>{ambient.variants.map(item => <div className="sound-row" key={item.id}><Button disabled={!item.available} onClick={() => { const audio = new Audio(item.url); audio.playbackRate = item.playback_rate; void audio.play(); window.setTimeout(() => audio.pause(), 4000); }}>Preview</Button><span>{item.label}<small>{item.source_path} · {item.playback_rate}×{!item.available ? " · missing" : ""}</small></span><Checkbox checked={item.enabled} onChange={e => void saveVariant(item, { enabled: e.target.checked })} /><TextField size="small" type="number" label="Gain" inputProps={{ min: 0, max: 1, step: .05 }} defaultValue={item.default_gain} onBlur={e => void saveVariant(item, { default_gain: Number(e.target.value) })} /></div>)}<Divider /><h3>Derived speed variant</h3><div className="route-form"><TextField select size="small" label="Source" value={derived.source_path} onChange={e => setDerived({ ...derived, source_path: e.target.value })}>{ambient.variants.filter(item => !item.derived).map(item => <MenuItem key={item.id} value={item.source_path}>{item.label}</MenuItem>)}</TextField><TextField size="small" label="New label" value={derived.label} onChange={e => setDerived({ ...derived, label: e.target.value })} /><TextField size="small" type="number" label="Speed" value={derived.playback_rate} onChange={e => setDerived({ ...derived, playback_rate: Number(e.target.value) })} /><Button onClick={() => void createDerived()}>Create variant</Button></div><h3>Action sounds</h3><div className="route-form"><TextField size="small" label="Action, e.g. running" value={actionRule.owner_id} onChange={e => setActionRule({ ...actionRule, owner_id: e.target.value })} /><TextField select size="small" label="Sound" value={actionRule.variant_id} onChange={e => setActionRule({ ...actionRule, variant_id: e.target.value })}>{ambient.variants.filter(item => item.enabled && item.available).map(item => <MenuItem key={item.id} value={item.id}>{item.label}</MenuItem>)}</TextField><Button onClick={() => void addActionRule()}>Assign</Button></div></section>;
}

function NoiseCatalog({ noises, saveNoise }: { noises: NoiseVariant[]; saveNoise: (item: NoiseVariant, patch: Partial<NoiseVariant>) => Promise<void> }) {
  return <section className="panel"><h2>One-shot noise catalog</h2><p>Files under <code>public/sounds/noises</code> can be played once by story and minigame systems. Ambient loops and Music are unaffected.</p>{!noises.length && <p className="muted">Add audio files to the noises folder to index them.</p>}{noises.map(item => <div className="sound-row" key={item.id}><Button disabled={!item.available} onClick={() => { const audio = new Audio(item.url); audio.playbackRate = item.playback_rate; audio.volume = item.default_gain; void audio.play(); }}>Preview</Button><span>{item.label}<small>{item.source_path} · {item.playback_rate}×{!item.available ? " · missing" : ""}</small></span><Checkbox checked={item.enabled} onChange={event => void saveNoise(item, { enabled: event.target.checked })} /><TextField size="small" type="number" label="Gain" inputProps={{ min: 0, max: 1, step: .05 }} defaultValue={item.default_gain} onBlur={event => void saveNoise(item, { default_gain: Number(event.target.value) })} /></div>)}</section>;
}

function descendantsOf(locationId: string | undefined, locations: WorldEntity[]): Set<string> { if (!locationId) return new Set(); const blocked = new Set([locationId]); let changed = true; while (changed) { changed = false; locations.forEach(item => { if (!blocked.has(item.id) && blocked.has(String(item.state.parent_location_id || ""))) { blocked.add(item.id); changed = true; } }); } return blocked; }
function composedPrompt(location: EnvironmentLocation, settings: EnvironmentSettings | null, weatherId?: string, phaseId?: string): string { if (!settings) return location.imagegen_description || location.description; const weather = settings.weather.find(item => item.id === (weatherId || settings.initial_weather_id)), phase = settings.time_phases.find(item => item.id === phaseId) ?? settings.time_phases.find(item => item.enabled); return [location.name, location.imagegen_description || location.description || "environment background", weather?.imagegen_description || weather?.description, phase?.imagegen_description || phase?.description, ...location.image_tags, ...(weather?.image_tags ?? [])].filter(Boolean).join(", "); }

export function LocationMap({ projectId, fail }: { projectId: string; fail: (message: string) => void }) { const [parentId, setParentId] = useState<string | null>(null); const [layer, setLayer] = useState<LocationMapLayer | null>(null); useEffect(() => { void api<LocationMapLayer>(`/projects/${projectId}/environment/map${parentId ? `?parent_id=${parentId}` : ""}`).then(setLayer).catch(cause => fail(String(cause))); }, [projectId, parentId, fail]); return <div className="player-map"><div className="sheet-heading"><h3>{layer?.parent?.name ?? "World map"}</h3>{layer?.parent && <Button onClick={() => setParentId(layer.parent?.parent_id ?? null)}>Go back</Button>}</div><div className="environment-map"><MapEdges layer={layer} />{layer?.locations.map(item => <button key={item.id} style={{ left: `${10 + item.x * 21}%`, top: `${12 + item.y * 24}%` }} onClick={() => item.has_children && setParentId(item.id)}>{item.name}</button>)}</div></div>; }
function MapEdges({ layer }: { layer: LocationMapLayer | null }) { if (!layer) return null; const byId = new Map(layer.locations.map(item => [item.id, item])); return <svg className="map-edges" viewBox="0 0 100 100" preserveAspectRatio="none">{layer.routes.map(route => { const from = byId.get(route.source_id), to = byId.get(route.target_id); return from && to ? <line key={route.id} x1={10 + from.x * 21} y1={12 + from.y * 24} x2={10 + to.x * 21} y2={12 + to.y * 24} /> : null; })}</svg>; }
