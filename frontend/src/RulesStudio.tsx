import { useCallback, useEffect, useMemo, useState } from "react";
import { Alert, Button, Checkbox, Dialog, DialogActions, DialogContent, DialogTitle, FormControlLabel, MenuItem, Stack, Switch, TextField } from "@mui/material";
import { api } from "./api";
import type { BulletCatalog } from "./BulletHellStudio";
import type { AbilityAction, AbilityActionTarget, AbilityCost, AbilityDefinition, EffectDefinition, FormulaNode, RequirementExpression, RuleMigrationWarning, RuleOwnerKind, StatDefinition } from "./types";

const ownerKinds: RuleOwnerKind[] = ["character", "item", "location", "faction", "lore_system", "fact", "plot_beat", "relationship"];
const actionTargets: AbilityActionTarget[] = ["actor", "target", "party", "location", "nearby_enemies", "faction_members", "relationship_target", "allies", "enemies", "all", "random"];
const entityKinds = ["character", "location", "faction", "item", "lore_system", "fact", "plot_beat"];
const blankStat: StatDefinition = { stat_key: "", label: "", description: "", compatible_owner_kinds: ["character"], default_value: 0, minimum: 0, maximum: 100, minimum_stat_key: null, maximum_stat_key: null, color: null, minimum_color: null, maximum_color: null, display_style: "compact", integer_only: true, visibility: "public" };
const blankEffect: EffectDefinition = { effect_key: "", name: "", description: "", target_stat_key: "", operation: "add", formula: { kind: "constant", value: 0 }, clock: "world_actions", duration: 0, tick_interval: 0, evaluation_mode: "snapshot", stacking_policy: "replace", max_stacks: 1, visibility: "public", icon: null, enabled: true };
const blankAbility: AbilityDefinition = { ability_key: "", name: "", description: "", ability_kind: "active", compatible_owner_kinds: ["character"], target_type: "self", requirements: {}, costs: [], actions: [], passive_triggers: [], icon: null, enabled: true, timed_attack_line_count: null, timed_attack_damage_per_line: null, bullethell_skill_ids: [] };

function copy<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
function blankCost(kind: AbilityCost["kind"], stats: StatDefinition[]): AbilityCost {
  if (kind === "stat") return { kind, stat_key: stats.find(item => item.compatible_owner_kinds.includes("character"))?.stat_key ?? "", amount: 1 };
  if (kind === "consume_fuel") return { kind, item_id: "", amount: 1 };
  return { kind, amount: 1 };
}
function blankAction(kind: AbilityAction["kind"], effects: EffectDefinition[]): AbilityAction {
  if (kind === "apply_effect") return { kind, target: "target", effect_key: effects[0]?.effect_key ?? "" };
  if (kind === "move") return { kind, target: "target", destination_id: "" };
  if (kind === "create") return { kind, target: "actor", entity_kind: "item", entity_name: "", state: {} };
  if (kind === "reveal_knowledge") return { kind, target: "target", fact_id: "" };
  if (kind === "change_relationship") return { kind, target: "target", relation: "" };
  if (kind === "advance_time") return { kind, target: "actor", minutes: 0 };
  if (kind === "play_noise") return { kind, target: "actor", noise_id: "" };
  return { kind, target: "target" };
}
function compatibleEffectsForAction(ability: AbilityDefinition, action: AbilityAction, effects: EffectDefinition[], stats: StatDefinition[]): EffectDefinition[] {
  const targetKind: RuleOwnerKind = action.target === "location" || (action.target === "target" && ability.target_type === "location")
    ? "location"
    : action.target === "target" && ability.target_type === "relationship"
      ? "relationship"
      : "character";
  return effects.filter(effect => stats.find(stat => stat.stat_key === effect.target_stat_key)?.compatible_owner_kinds.includes(targetKind));
}
function blankEffectAction(ability: AbilityDefinition, effects: EffectDefinition[], stats: StatDefinition[]): AbilityAction {
  const action: AbilityAction = { kind: "apply_effect", target: "target", effect_key: "" };
  return { ...action, effect_key: compatibleEffectsForAction(ability, action, effects, stats)[0]?.effect_key ?? "" };
}
function retargetAction(ability: AbilityDefinition, action: AbilityAction, target: AbilityActionTarget, effects: EffectDefinition[], stats: StatDefinition[]): AbilityAction {
  const next = { ...action, target };
  if (next.kind !== "apply_effect") return next;
  const compatible = compatibleEffectsForAction(ability, next, effects, stats);
  return compatible.some(effect => effect.effect_key === next.effect_key) ? next : { ...next, effect_key: compatible[0]?.effect_key ?? "" };
}

function FormulaEditor({ node, stats, onChange }: { node: FormulaNode; stats: StatDefinition[]; onChange: (node: FormulaNode) => void }) {
  const changeKind = (kind: FormulaNode["kind"]) => {
    if (kind === "constant") onChange({ kind, value: 0 });
    else if (kind === "stat") onChange({ kind, participant: "target", stat_key: stats[0]?.stat_key ?? "" });
    else if (kind === "negate") onChange({ kind, children: [{ kind: "constant", value: 0 }] });
    else onChange({ kind, children: [{ kind: "constant", value: 0 }, { kind: "constant", value: 0 }] });
  };
  return <div className="panel formula-node">
    <TextField select size="small" label="Formula node" value={node.kind} onChange={event => changeKind(event.target.value as FormulaNode["kind"])}>{["constant", "stat", "add", "subtract", "multiply", "divide", "minimum", "maximum", "negate"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>
    {node.kind === "constant" && <TextField size="small" type="number" label="Value" value={node.value} onChange={event => onChange({ ...node, value: Number(event.target.value) })} />}
    {node.kind === "stat" && <Stack direction="row" spacing={1}><TextField select size="small" label="Participant" value={node.participant} onChange={event => onChange({ ...node, participant: event.target.value as "actor" | "source" | "target" })}>{["actor", "source", "target"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><TextField select size="small" label="Stat" value={node.stat_key} onChange={event => onChange({ ...node, stat_key: event.target.value })}>{stats.map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField></Stack>}
    {"children" in node && node.children.map((child, index) => <FormulaEditor key={index} node={child} stats={stats} onChange={next => onChange({ ...node, children: node.children.map((value, childIndex) => childIndex === index ? next : value) } as FormulaNode)} />)}
  </div>;
}

function formulaReferences(node: FormulaNode): string[] { if (node.kind === "stat") return [`${node.participant}.${node.stat_key}`]; if (node.kind === "constant") return []; return node.children.flatMap(formulaReferences); }
function calculateFormula(node: FormulaNode, values: Record<string, number>): number {
  if (node.kind === "constant") return node.value;
  if (node.kind === "stat") return values[`${node.participant}.${node.stat_key}`] ?? 0;
  const operands = node.children.map(child => calculateFormula(child, values));
  if (node.kind === "negate") return -operands[0];
  if (node.kind === "add") return operands[0] + operands[1];
  if (node.kind === "subtract") return operands[0] - operands[1];
  if (node.kind === "multiply") return operands[0] * operands[1];
  if (node.kind === "divide") return operands[1] === 0 ? Number.NaN : operands[0] / operands[1];
  if (node.kind === "minimum") return Math.min(...operands);
  return Math.max(...operands);
}
function FormulaExample({ effect, stats }: { effect: EffectDefinition; stats: StatDefinition[] }) {
  const references = [...new Set(formulaReferences(effect.formula))];
  const defaults = Object.fromEntries(references.map(reference => [reference, stats.find(item => item.stat_key === reference.split(".")[1])?.default_value ?? 0]));
  const [values, setValues] = useState<Record<string, number>>(defaults);
  useEffect(() => setValues(defaults), [effect.formula]); // eslint-disable-line react-hooks/exhaustive-deps
  const result = calculateFormula(effect.formula, { ...defaults, ...values });
  return <section className="panel"><h3>Formula example</h3><Stack direction="row" spacing={1} flexWrap="wrap">{references.map(reference => <TextField key={reference} size="small" type="number" label={reference} value={values[reference] ?? defaults[reference]} onChange={event => setValues(current => ({ ...current, [reference]: Number(event.target.value) }))} />)}</Stack><p>{Number.isFinite(result) ? `Resolved magnitude: ${result}` : "The example is invalid (check division by zero)."}</p></section>;
}

export function RulesStudio({ projectId, revision, fail }: { projectId: string; revision: number; fail: (message: string) => void }) {
  const [rules, setRules] = useState<{ stats: StatDefinition[]; effects: EffectDefinition[]; abilities: AbilityDefinition[]; migration_warnings: RuleMigrationWarning[] }>({ stats: [], effects: [], abilities: [], migration_warnings: [] });
  const [stat, setStat] = useState<StatDefinition | null>(null); const [effect, setEffect] = useState<EffectDefinition | null>(null); const [ability, setAbility] = useState<AbilityDefinition | null>(null); const [editingKey, setEditingKey] = useState<string | null>(null); const [bulletSkills, setBulletSkills] = useState<Array<{ id: string; name: string }>>([]);
  const load = useCallback(async () => { const [next, catalog, settings] = await Promise.all([api<typeof rules>(`/projects/${projectId}/rules`), api<BulletCatalog>("/bullethell/catalog"), api<{ allowed_skill_ids: string[] }>(`/projects/${projectId}/bullethell`)]); setRules(next); setBulletSkills(catalog.skills.filter(item => settings.allowed_skill_ids.includes(item.id))); }, [projectId]);
  useEffect(() => { void load().catch(cause => fail(String(cause))); }, [load, revision, fail]);
  async function save(kind: "stats" | "effects" | "abilities", value: StatDefinition | EffectDefinition | AbilityDefinition) { try { await api(`/projects/${projectId}/${kind}${editingKey ? `/${editingKey}` : ""}`, { method: editingKey ? "PUT" : "POST", body: JSON.stringify(value) }); setStat(null); setEffect(null); setAbility(null); setEditingKey(null); await load(); } catch (cause) { fail(String(cause)); } }
  async function remove(kind: "stats" | "effects" | "abilities", key: string) { try { await api(`/projects/${projectId}/${kind}/${key}`, { method: "DELETE" }); await load(); } catch (cause) { fail(String(cause)); } }
  const compatibleBounds = useMemo(() => stat ? rules.stats.filter(item => item.stat_key !== stat.stat_key && stat.compatible_owner_kinds.every(kind => item.compatible_owner_kinds.includes(kind))) : [], [rules.stats, stat]);
  return <div className="page">
    <header className="page-header"><p className="eyebrow">CANONICAL RULES</p><h1>Stats, effects, and abilities</h1><p>Reusable effects calculate against actor, source, and target stats; abilities compose those effects with typed actions.</p></header>
    {rules.migration_warnings.filter(item => !item.acknowledged).map(item => <Alert key={item.id} severity="warning" action={<Button onClick={async () => { await api(`/projects/${projectId}/rules/migration-warnings/${item.id}/acknowledge`, { method: "POST" }); await load(); }}>Acknowledge</Button>}>{item.message}</Alert>)}
    <div className="rules-grid"><RuleList title="Stats" add={() => { setEditingKey(null); setStat(copy(blankStat)); }} rows={rules.stats.map(item => ({ key: item.stat_key, title: item.label, subtitle: `${item.stat_key} · ${item.compatible_owner_kinds.join(", ")}`, edit: () => { setEditingKey(item.stat_key); setStat(copy(item)); }, remove: () => void remove("stats", item.stat_key) }))}/><RuleList title="Effects" add={() => { setEditingKey(null); setEffect({ ...copy(blankEffect), target_stat_key: rules.stats[0]?.stat_key ?? "" }); }} rows={rules.effects.map(item => ({ key: item.effect_key, title: item.name, subtitle: `${item.operation} ${item.target_stat_key} · ${item.clock}`, edit: () => { setEditingKey(item.effect_key); setEffect(copy(item)); }, remove: () => void remove("effects", item.effect_key) }))}/><RuleList title="Abilities" add={() => { setEditingKey(null); setAbility(copy(blankAbility)); }} rows={rules.abilities.map(item => ({ key: item.ability_key, title: item.name, subtitle: `${item.ability_kind} · ${item.target_type} · ${item.actions.length} action(s)`, edit: () => { setEditingKey(item.ability_key); setAbility(copy(item)); }, remove: () => void remove("abilities", item.ability_key) }))}/></div>
    <Dialog open={Boolean(stat)} onClose={() => setStat(null)} maxWidth="md" fullWidth><DialogTitle>{editingKey ? "Edit" : "Add"} stat</DialogTitle><DialogContent className="music-dialog">{stat && <><TextField label="Stable key" disabled={Boolean(editingKey)} value={stat.stat_key} onChange={event => setStat({ ...stat, stat_key: event.target.value })}/><TextField label="Label" value={stat.label} onChange={event => setStat({ ...stat, label: event.target.value })}/><TextField multiline label="Description" value={stat.description} onChange={event => setStat({ ...stat, description: event.target.value })}/><Stack direction="row" flexWrap="wrap">{ownerKinds.map(kind => <FormControlLabel key={kind} control={<Checkbox checked={stat.compatible_owner_kinds.includes(kind)} onChange={event => setStat({ ...stat, compatible_owner_kinds: event.target.checked ? [...stat.compatible_owner_kinds, kind] : stat.compatible_owner_kinds.filter(value => value !== kind) })}/>} label={kind.replaceAll("_", " ")}/>)}</Stack><Stack direction="row" spacing={1}><TextField type="number" label="Default" value={stat.default_value} onChange={event => setStat({ ...stat, default_value: Number(event.target.value) })}/><TextField type="number" label="Minimum" value={stat.minimum} onChange={event => setStat({ ...stat, minimum: Number(event.target.value) })}/><TextField type="number" label="Maximum" value={stat.maximum} onChange={event => setStat({ ...stat, maximum: Number(event.target.value) })}/></Stack><Stack direction="row" spacing={1}><TextField select fullWidth label="Minimum from stat" value={stat.minimum_stat_key ?? ""} onChange={event => setStat({ ...stat, minimum_stat_key: event.target.value || null })}><MenuItem value="">Numeric minimum</MenuItem>{compatibleBounds.map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField><TextField select fullWidth label="Maximum from stat" value={stat.maximum_stat_key ?? ""} onChange={event => setStat({ ...stat, maximum_stat_key: event.target.value || null })}><MenuItem value="">Numeric maximum</MenuItem>{compatibleBounds.map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField></Stack><Stack direction="row" spacing={1}><TextField label="Color" value={stat.color ?? ""} onChange={event => setStat({ ...stat, color: event.target.value || null })}/><TextField label="Minimum color" value={stat.minimum_color ?? ""} onChange={event => setStat({ ...stat, minimum_color: event.target.value || null })}/><TextField label="Maximum color" value={stat.maximum_color ?? ""} onChange={event => setStat({ ...stat, maximum_color: event.target.value || null })}/></Stack><Stack direction="row" spacing={1}><TextField select label="Display" value={stat.display_style} onChange={event => setStat({ ...stat, display_style: event.target.value as "compact" | "bar" })}><MenuItem value="compact">Compact</MenuItem><MenuItem value="bar">Bar</MenuItem></TextField><TextField select label="Visibility" value={stat.visibility} onChange={event => setStat({ ...stat, visibility: event.target.value as StatDefinition["visibility"] })}>{["public", "private", "narrator"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><FormControlLabel control={<Switch checked={stat.integer_only} onChange={event => setStat({ ...stat, integer_only: event.target.checked })}/>} label="Integer only"/></Stack></>}</DialogContent><DialogActions><Button onClick={() => setStat(null)}>Cancel</Button><Button variant="contained" onClick={() => stat && void save("stats", stat)}>Save</Button></DialogActions></Dialog>
    <Dialog open={Boolean(effect)} onClose={() => setEffect(null)} maxWidth="md" fullWidth><DialogTitle>{editingKey ? "Edit" : "Add"} effect</DialogTitle><DialogContent className="music-dialog">{effect && <><TextField label="Stable key" disabled={Boolean(editingKey)} value={effect.effect_key} onChange={event => setEffect({ ...effect, effect_key: event.target.value })}/><TextField label="Name" value={effect.name} onChange={event => setEffect({ ...effect, name: event.target.value })}/><TextField multiline label="Description" value={effect.description} onChange={event => setEffect({ ...effect, description: event.target.value })}/><TextField label="Icon media slot" value={effect.icon ?? ""} onChange={event => setEffect({ ...effect, icon: event.target.value || null })}/><Stack direction="row" spacing={1}><TextField select fullWidth label="Target stat" value={effect.target_stat_key} onChange={event => setEffect({ ...effect, target_stat_key: event.target.value })}>{rules.stats.map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField><TextField select label="Operation" value={effect.operation} onChange={event => setEffect({ ...effect, operation: event.target.value as EffectDefinition["operation"] })}>{["add", "subtract", "set", "multiply"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField></Stack><FormulaEditor node={effect.formula} stats={rules.stats} onChange={formula => setEffect({ ...effect, formula })}/><FormulaExample effect={effect} stats={rules.stats}/><Stack direction="row" spacing={1}><TextField select label="Clock" value={effect.clock} onChange={event => setEffect({ ...effect, clock: event.target.value as EffectDefinition["clock"] })}>{["story_minutes", "target_actions", "world_actions"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><TextField type="number" label="Duration (-1 indefinite)" value={effect.duration} onChange={event => setEffect({ ...effect, duration: Number(event.target.value) })}/><TextField type="number" label="Tick interval" value={effect.tick_interval} onChange={event => setEffect({ ...effect, tick_interval: Number(event.target.value) })}/></Stack><Stack direction="row" spacing={1}><TextField select label="Evaluation" value={effect.evaluation_mode} onChange={event => setEffect({ ...effect, evaluation_mode: event.target.value as EffectDefinition["evaluation_mode"] })}><MenuItem value="snapshot">Snapshot</MenuItem><MenuItem value="live">Live</MenuItem></TextField><TextField select label="Stacking" value={effect.stacking_policy} onChange={event => setEffect({ ...effect, stacking_policy: event.target.value as EffectDefinition["stacking_policy"] })}>{["replace", "refresh", "stack", "independent"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><TextField type="number" label="Max stacks" value={effect.max_stacks} onChange={event => setEffect({ ...effect, max_stacks: Number(event.target.value) })}/><TextField select label="Visibility" value={effect.visibility} onChange={event => setEffect({ ...effect, visibility: event.target.value as EffectDefinition["visibility"] })}>{["public", "private", "narrator"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField></Stack><FormControlLabel control={<Switch checked={effect.enabled} onChange={event => setEffect({ ...effect, enabled: event.target.checked })}/>} label="Enabled"/></>}</DialogContent><DialogActions><Button onClick={() => setEffect(null)}>Cancel</Button><Button variant="contained" onClick={() => effect && void save("effects", effect)}>Save</Button></DialogActions></Dialog>
    <AbilityDialog ability={ability} editing={Boolean(editingKey)} stats={rules.stats} effects={rules.effects} abilities={rules.abilities} bulletSkills={bulletSkills} setAbility={setAbility} close={() => setAbility(null)} save={() => ability && void save("abilities", ability)}/>
  </div>;
}

function RuleList({ title, add, rows }: { title: string; add: () => void; rows: Array<{ key: string; title: string; subtitle: string; edit: () => void; remove: () => void }> }) { return <section className="panel"><Stack direction="row" justifyContent="space-between"><h2>{title}</h2><Button onClick={add}>Add</Button></Stack>{rows.map(row => <article className="rule-row" key={row.key}><div><strong>{row.title}</strong><small>{row.subtitle}</small></div><div><Button onClick={row.edit}>Edit</Button><Button color="error" onClick={row.remove}>Delete</Button></div></article>)}</section>; }

const requirementKinds = ["and", "or", "not", "compare", "has_tag", "has_item", "has_ability", "relationship", "location", "time", "weather"] as const;
function blankRequirement(kind: NonNullable<RequirementExpression["kind"]>, stats: StatDefinition[]): RequirementExpression {
  if (kind === "and" || kind === "or") return { kind, children: [{ kind: "compare", target: "actor", stat_key: stats[0]?.stat_key ?? "", comparison: "gte", value: 0 }] };
  if (kind === "not") return { kind, child: { kind: "compare", target: "actor", stat_key: stats[0]?.stat_key ?? "", comparison: "gte", value: 0 } };
  if (kind === "compare") return { kind, target: "actor", stat_key: stats[0]?.stat_key ?? "", comparison: "gte", value: 0 };
  return { kind, target: "actor" };
}
function RequirementEditor({ node, stats, abilities, onChange, onRemove }: { node: RequirementExpression; stats: StatDefinition[]; abilities: AbilityDefinition[]; onChange: (value: RequirementExpression) => void; onRemove: () => void }) {
  const kind = node.kind ?? "compare";
  const children = node.children ?? [];
  return <section className="panel">
    <Stack direction="row" spacing={1} flexWrap="wrap">
      <TextField select size="small" label="Requirement" value={kind} onChange={event => onChange(blankRequirement(event.target.value as NonNullable<RequirementExpression["kind"]>, stats))}>{requirementKinds.map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>
      {!(["and", "or", "not"] as string[]).includes(kind) && <TextField select size="small" label="Participant" value={node.target ?? "actor"} onChange={event => onChange({ ...node, target: event.target.value as "actor" | "target" })}><MenuItem value="actor">actor</MenuItem><MenuItem value="target">target</MenuItem></TextField>}
      {kind === "compare" && <><TextField select size="small" label="Stat" value={node.stat_key ?? ""} onChange={event => onChange({ ...node, stat_key: event.target.value })}>{stats.map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField><TextField select size="small" label="Comparison" value={node.comparison ?? "gte"} onChange={event => onChange({ ...node, comparison: event.target.value as RequirementExpression["comparison"] })}>{["eq", "ne", "lt", "lte", "gt", "gte"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><TextField size="small" type="number" label="Value" value={Number(node.value ?? 0)} onChange={event => onChange({ ...node, value: Number(event.target.value) })}/></>}
      {kind === "has_tag" && <TextField size="small" label="Tag" value={node.tag ?? ""} onChange={event => onChange({ ...node, tag: event.target.value })}/>} 
      {kind === "has_item" && <TextField size="small" label="Item ID" value={node.item_id ?? ""} onChange={event => onChange({ ...node, item_id: event.target.value })}/>} 
      {kind === "has_ability" && <TextField select size="small" label="Ability" value={node.ability_key ?? ""} onChange={event => onChange({ ...node, ability_key: event.target.value })}>{abilities.map(item => <MenuItem key={item.ability_key} value={item.ability_key}>{item.name}</MenuItem>)}</TextField>}
      {kind === "relationship" && <TextField size="small" label="Relationship" value={node.relation ?? ""} onChange={event => onChange({ ...node, relation: event.target.value })}/>} 
      {kind === "location" && <TextField size="small" label="Location ID" value={node.location_id ?? ""} onChange={event => onChange({ ...node, location_id: event.target.value })}/>} 
      {kind === "time" && <TextField size="small" label="Time phase ID" value={node.time_phase_id ?? ""} onChange={event => onChange({ ...node, time_phase_id: event.target.value })}/>} 
      {kind === "weather" && <TextField size="small" label="Weather ID" value={node.weather_id ?? ""} onChange={event => onChange({ ...node, weather_id: event.target.value })}/>} 
      <Button color="error" onClick={onRemove}>Remove</Button>
    </Stack>
    {(kind === "and" || kind === "or") && <Stack spacing={1}>{children.map((child, index) => <RequirementEditor key={index} node={child} stats={stats} abilities={abilities} onChange={value => onChange({ ...node, children: children.map((item, childIndex) => childIndex === index ? value : item) })} onRemove={() => onChange({ ...node, children: children.filter((_, childIndex) => childIndex !== index) })}/>)}<Button onClick={() => onChange({ ...node, children: [...children, blankRequirement("compare", stats)] })}>Add child</Button></Stack>}
    {kind === "not" && <RequirementEditor node={node.child ?? blankRequirement("compare", stats)} stats={stats} abilities={abilities} onChange={child => onChange({ ...node, child })} onRemove={() => onChange({ ...node, child: blankRequirement("compare", stats) })}/>} 
  </section>;
}
function AbilityDialog({ ability, editing, stats, effects, abilities, bulletSkills, setAbility, close, save }: { ability: AbilityDefinition | null; editing: boolean; stats: StatDefinition[]; effects: EffectDefinition[]; abilities: AbilityDefinition[]; bulletSkills: Array<{ id: string; name: string }>; setAbility: (value: AbilityDefinition) => void; close: () => void; save: () => void }) {
  if (!ability) return null;
  const updateCost = (index: number, patch: Partial<AbilityCost>) => setAbility({ ...ability, costs: ability.costs.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item) });
  const updateAction = (index: number, patch: Partial<AbilityAction>) => setAbility({ ...ability, actions: ability.actions.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item) });
  return <Dialog open onClose={close} maxWidth="lg" fullWidth><DialogTitle>{editing ? "Edit" : "Add"} ability</DialogTitle><DialogContent className="music-dialog"><TextField label="Stable key" disabled={editing} value={ability.ability_key} onChange={event => setAbility({ ...ability, ability_key: event.target.value })}/><TextField label="Name" value={ability.name} onChange={event => setAbility({ ...ability, name: event.target.value })}/><TextField multiline label="Description" value={ability.description} onChange={event => setAbility({ ...ability, description: event.target.value })}/><TextField label="Icon media slot" value={ability.icon ?? ""} onChange={event => setAbility({ ...ability, icon: event.target.value || null })}/><Stack direction="row" spacing={1}><TextField select label="Kind" value={ability.ability_kind} onChange={event => setAbility({ ...ability, ability_kind: event.target.value as "active" | "passive" })}><MenuItem value="active">Active</MenuItem><MenuItem value="passive">Passive</MenuItem></TextField><TextField select label="Target" value={ability.target_type} onChange={event => setAbility({ ...ability, target_type: event.target.value as AbilityDefinition["target_type"] })}>{["self", "character", "choice", "relationship", "location", "all", "party", "allies", "enemies", "nearby_enemies", "faction_members", "random"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>{(["character", "item"] as const).map(kind => <FormControlLabel key={kind} control={<Checkbox checked={ability.compatible_owner_kinds.includes(kind)} onChange={event => setAbility({ ...ability, compatible_owner_kinds: event.target.checked ? [...ability.compatible_owner_kinds, kind] : ability.compatible_owner_kinds.filter(value => value !== kind) })}/>} label={kind}/>)}</Stack>
    <h3>Requirements</h3>{ability.requirements?.kind ? <RequirementEditor node={ability.requirements} stats={stats} abilities={abilities} onChange={requirements => setAbility({ ...ability, requirements })} onRemove={() => setAbility({ ...ability, requirements: {} })}/> : <Button onClick={() => setAbility({ ...ability, requirements: blankRequirement("and", stats) })}>Add requirement tree</Button>}
    <h3>Costs</h3>{ability.costs.map((cost, index) => <Stack key={index} direction="row" spacing={1}><TextField select label="Cost" value={cost.kind} onChange={event => setAbility({ ...ability, costs: ability.costs.map((item, itemIndex) => itemIndex === index ? blankCost(event.target.value as AbilityCost["kind"], stats) : item) })}>{["stat", "consume_source", "consume_fuel"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>{cost.kind === "stat" && <TextField select label="Stat" value={cost.stat_key ?? ""} onChange={event => updateCost(index, { stat_key: event.target.value })}>{stats.filter(item => item.compatible_owner_kinds.includes("character")).map(item => <MenuItem key={item.stat_key} value={item.stat_key}>{item.label}</MenuItem>)}</TextField>}{cost.kind === "consume_fuel" && <TextField label="Fuel item ID" value={cost.item_id ?? ""} onChange={event => updateCost(index, { item_id: event.target.value })}/>}<TextField type="number" label="Amount" value={cost.amount} onChange={event => updateCost(index, { amount: Number(event.target.value) })}/><Button onClick={() => setAbility({ ...ability, costs: ability.costs.filter((_, itemIndex) => itemIndex !== index) })}>Remove</Button></Stack>)}<Button disabled={!stats.length} onClick={() => setAbility({ ...ability, costs: [...ability.costs, blankCost("stat", stats)] })}>Add cost</Button>
    <h3>Ordered actions</h3>
    {ability.actions.map((action, index) => <Stack key={index} direction="row" spacing={1} flexWrap="wrap">
      <TextField select label="Action" value={action.kind} onChange={event => {
        const kind = event.target.value as AbilityAction["kind"];
        const next = kind === "apply_effect" ? blankEffectAction(ability, effects, stats) : blankAction(kind, effects);
        setAbility({ ...ability, actions: ability.actions.map((item, itemIndex) => itemIndex === index ? next : item) });
      }}>{["apply_effect", "move", "create", "remove", "reveal_knowledge", "change_relationship", "advance_time", "play_noise"].map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>
      <TextField select label="Target" value={action.target} onChange={event => setAbility({ ...ability, actions: ability.actions.map((item, itemIndex) => itemIndex === index ? retargetAction(ability, action, event.target.value as AbilityActionTarget, effects, stats) : item) })}>{actionTargets.map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>
      {action.kind === "apply_effect" && <><TextField select label="Effect" value={action.effect_key ?? ""} onChange={event => updateAction(index, { effect_key: event.target.value })}>{compatibleEffectsForAction(ability, action, effects, stats).map(item => <MenuItem key={item.effect_key} value={item.effect_key}>{item.name}</MenuItem>)}</TextField><TextField type="number" label="Duration override" value={action.duration_override ?? ""} onChange={event => updateAction(index, { duration_override: event.target.value === "" ? null : Number(event.target.value) })}/><TextField type="number" label="Tick override" value={action.tick_override ?? ""} onChange={event => updateAction(index, { tick_override: event.target.value === "" ? null : Number(event.target.value) })}/></>}
      {action.kind === "move" && <TextField label="Destination location ID" value={action.destination_id ?? ""} onChange={event => updateAction(index, { destination_id: event.target.value })}/>} 
      {action.kind === "create" && <><TextField select label="Entity kind" value={action.entity_kind ?? "item"} onChange={event => updateAction(index, { entity_kind: event.target.value })}>{entityKinds.map(value => <MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField><TextField label="Entity name" value={action.entity_name ?? ""} onChange={event => updateAction(index, { entity_name: event.target.value })}/></>} 
      {action.kind === "reveal_knowledge" && <TextField label="Fact ID" value={action.fact_id ?? ""} onChange={event => updateAction(index, { fact_id: event.target.value })}/>} 
      {action.kind === "change_relationship" && <TextField label="Relationship type" value={action.relation ?? ""} onChange={event => updateAction(index, { relation: event.target.value })}/>} 
      {action.kind === "advance_time" && <TextField type="number" label="Minutes" value={action.minutes ?? 0} onChange={event => updateAction(index, { minutes: Number(event.target.value) })}/>} 
      {action.kind === "play_noise" && <TextField label="Noise ID" value={action.noise_id ?? ""} onChange={event => updateAction(index, { noise_id: event.target.value })}/>} 
      <Button onClick={() => setAbility({ ...ability, actions: ability.actions.filter((_, itemIndex) => itemIndex !== index) })}>Remove</Button>
    </Stack>)}
    <Button onClick={() => setAbility({ ...ability, actions: [...ability.actions, effects.length ? blankEffectAction(ability, effects, stats) : blankAction("advance_time", effects)] })}>Add action</Button>
    {ability.ability_kind === "passive" && <><h3>Passive triggers</h3>{["ability_used", "stat_changed", "damage", "owner_action", "movement", "time_advanced"].map(kind => <FormControlLabel key={kind} control={<Checkbox checked={ability.passive_triggers.some(item => item.kind === kind)} onChange={event => setAbility({ ...ability, passive_triggers: event.target.checked ? [...ability.passive_triggers, { kind: kind as AbilityDefinition["passive_triggers"][number]["kind"] }] : ability.passive_triggers.filter(item => item.kind !== kind) })}/>} label={kind}/>)}</>}
    <h3>Minigames</h3><Stack direction="row" spacing={1}><TextField type="number" label="Timed attack lines" value={ability.timed_attack_line_count ?? ""} onChange={event => setAbility({ ...ability, timed_attack_line_count: event.target.value === "" ? null : Number(event.target.value) })}/><TextField type="number" label="Damage per line" value={ability.timed_attack_damage_per_line ?? ""} onChange={event => setAbility({ ...ability, timed_attack_damage_per_line: event.target.value === "" ? null : Number(event.target.value) })}/></Stack><Stack direction="row" flexWrap="wrap">{bulletSkills.map(skill => <FormControlLabel key={skill.id} control={<Checkbox checked={ability.bullethell_skill_ids.includes(skill.id)} onChange={event => setAbility({ ...ability, bullethell_skill_ids: event.target.checked ? [...ability.bullethell_skill_ids, skill.id] : ability.bullethell_skill_ids.filter(id => id !== skill.id) })}/>} label={skill.name}/>)}</Stack><FormControlLabel control={<Switch checked={ability.enabled} onChange={event => setAbility({ ...ability, enabled: event.target.checked })}/>} label="Enabled"/>
  </DialogContent><DialogActions><Button onClick={close}>Cancel</Button><Button variant="contained" onClick={save}>Save</Button></DialogActions></Dialog>;
}
